import React, { ReactElement } from "react";
import Layout from "../components/layout";

function HomePage(): ReactElement {
    return (
        <Layout>
            <h1>Arne Lapõnin's page is coming soon!</h1>
        </Layout>
    );
}

export default HomePage;
